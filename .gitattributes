# Keep line endings identical on a Windows laptop and a Linux cluster, so a
# checkout on one machine is byte-identical to a checkout on the other. The
# git commit hash is part of the provenance record; the files it points at
# should not differ by platform.
* text=auto eol=lf
